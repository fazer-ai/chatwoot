type: object
properties:
  additional_attributes:
    type: object
    description: The object containing additional attributes related to the contact
    properties:
      city:
        type: string
        description: City of the contact
      country:
        type: string
        description: Country of the contact
      country_code:
        type:
          - string
          - 'null'
        description: Country code of the contact
      created_at_ip:
        type: string
        description: IP address when the contact was created
  custom_attributes:
    type: object
    description: The custom attributes of the contact
  email:
    type: string
    description: The email address of the contact
  id:
    type: integer
    description: The ID of the contact
  identifier:
    type:
      - string
      - 'null'
    description: The identifier of the contact
  name:
    type: string
    description: The name of the contact
  phone_number:
    type:
      - string
      - 'null'
    description: The phone number of the contact
  thumbnail:
    type: string
    description: The thumbnail of the contact
  blocked:
    type: boolean
    description: Whether the contact is blocked
  language_id:
    type:
      - integer
      - 'null'
    description: ID of the language the contact prefers. `null` if no preference is set. See `GET /api/v1/languages`.
    example: 3
  language_code:
    type:
      - string
      - 'null'
    description: Denormalized language code (e.g. `pt-br`) derived from the contact's language. Read-only — set via `language_id`.
    example: es-es
  type:
    type: string
    description: The type of entity
    enum: ["contact"]